# Frontend Environment Configuration
# Copy this file to .env and adjust values as needed

# Backend API base URL
# Use /api/v1 (relative) in dev so requests go through Vite's proxy (avoids CORS)
# Use full URL (e.g. https://your-domain.com/api/v1) in production builds
VITE_API_BASE=/api/v1

# ML Server base URL
VITE_ML_BASE=http://localhost:5001
VITE_ML_PORT=5001

# ML API Key for authentication (required in production)
# Get this from your deployment or ML service provider
VITE_ML_API_KEY=your-secure-ml-api-key
